#include<stdio.h>
int main(){
	int n,d,sum=0,temp;
	printf("enter the number");
	scanf("%d",&n);
	temp=n;
	while(n>0){
		d=n%10;
		sum=sum+d*d*d;
		n=n/10;
}
	if(temp==sum)
	  printf("armstrong number");
	else
	  printf("not an armstrong number");
	
	return 0;
}
